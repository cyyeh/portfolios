{
	"type": "module",
	"scripts": {
		"build": "mkdir -p dist && node index.js > dist/index.html",
		"deploy": "pnpm build && netlify deploy"
	},
	"devDependencies": {
		"js-yaml": "^4.1.0",
		"netlify-cli": "^9.3.0",
		"puppeteer": "^13.1.3"
	}
}
