{
	"type": "module",
	"scripts": {
		"build": "mkdir -p dist && node index.js > dist/index.html",
		"deploy": "gh-pages -d dist"
	},
	"devDependencies": {
		"gh-pages": "^3.2.3",
		"js-yaml": "^4.1.0",
		"puppeteer": "^13.1.3"
	}
}
